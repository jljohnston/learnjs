


// Assigning a function to a variable (Still called the same, e.g. square(4);
var square = function(number) {
    return number * number;
}

// TODO: Create a function assigned to a variable that can do an exponent of and use in the inline JavaScript in 'chapter three.html'
